/*
#include "bitshake.h"

void std_bitshake(char   *cnt,
                  size_t len,
                  char   *key,
                  bool   cipher)
{
  int i;
  int bit;
  int index;

  i = 0;
  if (cipher == true)
    {
      while (i < len)
	{
	  bit = ;//ici je dois prendre les 3 premiers bits
	  index = ;//ici c'est la 5 bits suivant

	  cnt[i] = ;//
	  cnt[(i + index) % len] = ;//ici j'échange les 
	  i++;
	}
    }
  else
    {
    }
}

//on ne change que les 4 premiers bit dans cette exercice, les 4 dernier définice si c'est une majuscule ou une minuscule
//les 3 premier bit change,  et les 5 suivant servent d'adresse
*/
